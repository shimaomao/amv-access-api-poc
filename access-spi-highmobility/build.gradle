description = 'Highmobility implementation of the AMV access service provider interface'

dependencies {
    compile project(':access-spi')
    compile 'org.amv.highmobility:amv-highmobility-cryptotool-wrapper:0.0.8'

    compile 'org.springframework.boot:spring-boot-starter'
}